apiVersion: v1
kind: Service
metadata:
  name: {{ include "dionysus-planner.fullname" . }}
  labels:
    {{- include "dionysus-planner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dionysus-planner.selectorLabels" . | nindent 4 }}
